# Studio residual audit simulations

These simulation overrides were used for bounded A/B diagnostics during the
Studio `ours` residual migration.  They are intentionally separate from the
production `ours` learning configuration and must not be used for a training
gate or a batch launch.
